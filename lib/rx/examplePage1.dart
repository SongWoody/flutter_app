
import 'package:flutter/material.dart';

class ExamplePage1 extends StatelessWidget {
  const ExamplePage1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("ExamplePage1"),),
      body: Container(),
    );
  }
}
