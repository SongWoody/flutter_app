

import 'package:flutter/material.dart';

class FirebaseExamplePage extends StatelessWidget {
  const FirebaseExamplePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
